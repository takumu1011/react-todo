import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <h1>Todoアプリ(投稿、完了、削除)</h1>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
